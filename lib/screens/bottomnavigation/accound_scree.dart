import 'package:flutter/material.dart';

class AccoundScreen extends StatelessWidget {
  const AccoundScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('AccoundScreen'),),
    );
  }
}